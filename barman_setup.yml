---
- hosts: barman
  tasks:
    - name: add source repository into source list
      apt_repository:
        repo: deb http://apt.postgresql.org/pub/repos/apt/ jessie-pgdg main
        state: present
        filename: '/etc/apt/sources.list.d/pgdg.list'

    - name: import the repository signing key
      apt_key:
        url: "https://www.postgresql.org/media/keys/ACCC4CF8.asc"
        state: present

    - name: update the package lists
      apt: 
        update_cache: yes

    - name: Install Barman
      apt:
        name: barman

    - name: Install postgresql
      apt:
        name: postgresql-9.6

    - name: install repmgr
      apt:
        name: repmgr
        force: yes

    - name: install barman-cli
      apt: 
        name: barman-cli

    - name: change the hostname
      hostname:
        name: "{{hostname}}"

    - name: add new hostname in /etc/hosts
      lineinfile:
        dest: /etc/hosts
        line: '127.0.0.1 {{hostname}} {{hostname}}' 

    - name: Copy barman server configuration into /etc/barman.d
      copy: 
        src: config/barman/barman.conf
        dest: /etc/

    - name: Copy database server pg 's configuration into /etc/barman.d
      copy: 
        src: config/barman/pg.conf
        dest: /etc/barman.d/

    - name: Copy database server standby 's configuration into /etc/barman.d
      copy: 
        src: config/barman/standby1.conf
        dest: /etc/barman.d/

    - name: install pip
      command: aptitude install -y python-pip

    - name: install python-pexpect
      command: pip install pexpect
    
    - name: generate ssh keys
      expect: 
        command: ssh-keygen -t rsa
        responses:
          'Enter file in which to save the key \(/var/lib/barman/.ssh/id_rsa\):': "\n"
          'Enter passphrase \(empty for no passphrase\):': "\n"
          'Enter same passphrase again:': "\n"
      become: true
      become_method: sudo
      become_user: barman

    - name: fetch the public key of barman to store in the local box
      fetch:
        src: /var/lib/barman/.ssh/id_rsa.pub
        dest: tmp

    - name: create incoming wal directory for master server
      file:
        path: /var/lib/barman/pg/incoming
        state: directory
        owner: barman
        group: barman
        mode: 0755

    - name: create incoming wal directory for standby server
      file:
        path: /var/lib/barman/standby1/incoming
        state: directory
        owner: barman
        group: barman
        mode: 0755